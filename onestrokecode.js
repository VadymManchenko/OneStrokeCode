let putin = {message: "йди за руським кораблем"}, translations = ["go by russian warship", "va avec le navire de guerre russe", "geh mit dem russischen Kriegsschiff", "ロシアの軍艦を追いかける", "vai con la nave da guerra russa"], delay = (ms) => new Promise(r => setTimeout(r, ms)), printMessage = async msg => { for (let i = 0; i < msg.length; i++) { process.stdout.write(msg[i]); await delay(50); } console.log(""); }; (async () => { for (let i = 0; i < translations.length + 1; i++) { await delay(i * 1500); await printMessage(${i+1}. putin : ${i === 0 ? putin.message : translations[i-1]}); } console.log("Слава Україні!"); })();